<%= @appointment.appointment_datetime.strftime("%H:%M %B %e, %Y") %>
<%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor.id) %>
<%= link_to @appointment.patient.name, patient_path(@appointment.patient.id)  %>