<%= @appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M") %>
<%= link_to @appointment.doctor_name, doctor_path(@appointment.doctor.id) %>
<%= link_to @appointment.patient_name, patient_path(@appointment.patient.id) %>patient
