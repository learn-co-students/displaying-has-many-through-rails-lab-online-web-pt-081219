<h1><%= @patient.name %></h1>

<ul>
    <% @patient.appointments.each do |a| %>
        <li><%= link_to "#{a.doctor.name} - #{a.appointment_datetime.strftime("%B %d, %Y at %H:%M")}", doctor_path(a.doctor) %></li>
    <% end %>
</ul>